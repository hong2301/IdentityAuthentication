{
  "name": "card_reader",
  "version": "1.0.0",
  "description": "Node.js addon for card_reader",
  "scripts": {
    "build": "cd build && cmake .. &&cmake --build .",
    "test": "node test/test.js"
  },
  "dependencies": {
    "node-addon-api": "^8.5.0"
  }
}
